{
  "name": "rails-in-browser",
  "version": "0.1.0",
  "description": "Rails complet dans le navigateur : VM Linux x86 (CheerpX) + pont HTTP Service Worker + persistance IndexedDB",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node serve.mjs",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "v86": "^0.5.432"
  }
}
